# 07-06-03. 해제 연출 E-08

담당: **AGENT**
상위: [07-06.result-screen.md](07-06.result-screen.md) · 계약: [../UI-GUIDE.md](../UI-GUIDE.md) 1절 "축하는 절제", 2.2, 3.1~3.2, 3.3(E-08), 6절 N-05
선행: [07-06-02.word-result-cards.md](07-06-02.word-result-cards.md) · 다음: [07-07-01.route-transitions.md](07-07-01.route-transitions.md)
불변: INV-04b, INV-05, INV-08, INV-09, INV-11, INV-12
커밋: `feat: 레벨 해제 시 조각 낙하 연출 1회`

## 목표

첫 제출로 다음 레벨이 열렸을 때만, 헤더 위로 사각 조각 24개가 **1회** 떨어진다. 외부 컨페티 패키지 없이 자체 `CustomPainter`로 그린다(N-05, INV-11).
이 문서가 07-06 묶음의 마지막이다. `test/ui/result_screen_test.dart` 11종을 여기서 확정한다.

## 컨텍스트 (시작 전 읽기)

- `app/lib/ui/result/result_page.dart` — `justUnlocked` 계산(`result.isFirstSubmit && result.score >= spec.clearScore && next != null`), 07-06-02가 넣은 `_ResultPageState`
- `app/lib/ui/result/score_header.dart` — 해제 배지 위치(연출은 이 헤더 영역 위에 얹힌다)
- `app/lib/domain/model/level_spec.dart` — `clearScore` 기본값 0
- `app/test/ui/submit_test.dart` — `레벨 해제: score >= clearScore(첫 제출) → 안내 표시`, `레벨 해제 안 됨…`, `레벨 해제 조건이어도 재제출이면 안내 없음`(INV-08 보호)
- `app/test/ui/result_screen_test.dart` — 07-06-01·02가 넣은 7종

## 산출물

```
app/lib/ui/result/unlock_burst.dart     신규 — UnlockBurst, BurstPiece, buildBurstPieces, _BurstPainter
app/lib/ui/result/result_page.dart      헤더를 Stack으로 감싸 연출을 얹는다
app/test/ui/result_screen_test.dart     이 문서 몫 4종 추가 → 최종 11종 확정
```

## 작업

### 1. 표시 조건

`ResultPage`가 이미 계산해 둔 `justUnlocked` 하나로 끝난다. 새로 판단하지 않는다.

| 상황 | `isFirstSubmit` | `score >= clearScore` | `next` | 연출 |
|---|---|---|---|---|
| 첫 제출로 해제 | true | true | 있음 | **표시** |
| 재제출 | false | true | 있음 | 없음 |
| 점수 미달 | true | false | 있음 | 없음 |
| 마지막 레벨 | true | true | `null` | 없음 |

해제 배지 `'레벨 $id이 열렸습니다'`(accent 알약 + `cellInkOnAccent` 글자, `GameType.label`)는 07-06-01이 이미 넣었다. 이 문서는 **배지 문구·스타일을 바꾸지 않는다.**

### 2. 조각 정의 — 결정적

```dart
const int _pieceCount = 24;        // UI-GUIDE 3.3 E-08 "사각 조각 24개"
const double _pieceW = 6, _pieceH = 10;   // 07-06 부모 문서 도식
const double _startY = 20, _overshoot = 40;
const double _maxDelay = 0.2, _fadeStart = 0.7;

/// 전 필드 `operator ==`/`hashCode`를 붙인다 — 결정성 테스트가 두 리스트를
/// `equals`로 비교한다.
@immutable
class BurstPiece {
  final double xRatio;   // 0~1. 실제 x = xRatio * 폭
  final double fall;     // 낙하 속도 배수
  final double spin;     // 회전 바퀴 수 (부호가 방향)
  final double delay;    // 0~0.2, 진행값 기준
  final int colorIndex;  // 0 ink / 1 accent / 2 accentSoft
}

/// `Random(42)` 고정 시드라 실행·플랫폼과 무관하게 같은 배치가 나온다
/// (07-06 "결정성", 골든 재현성). `Random()`(시드 없음)은 쓰지 않는다.
List<BurstPiece> buildBurstPieces() {
  final rnd = Random(42);
  return [
    for (var i = 0; i < _pieceCount; i++)
      BurstPiece(
        xRatio: rnd.nextDouble(),
        fall: 0.85 + rnd.nextDouble() * 0.3,
        spin: rnd.nextDouble() * 4 - 2,
        delay: rnd.nextDouble() * _maxDelay,
        colorIndex: i % 3,            // 24 / 3 → ink·accent·accentSoft 정확히 8개씩
      ),
  ];
}

final List<BurstPiece> kBurstPieces = buildBurstPieces();
```

### 3. `_BurstPainter`

```dart
class _BurstPainter extends CustomPainter {
  final double t;                       // 0 → 1
  final Color ink, accent, accentSoft;  // List로 받으면 shouldRepaint 비교가 항상 true다
  const _BurstPainter({required this.t, required this.ink,
      required this.accent, required this.accentSoft});

  @override
  void paint(Canvas canvas, Size size) {
    final palette = [ink, accent, accentSoft];
    final alpha = t <= _fadeStart ? 1.0 : (1 - (t - _fadeStart) / (1 - _fadeStart)).clamp(0.0, 1.0);
    for (final p in kBurstPieces) {
      final local = ((t - p.delay) / (1 - p.delay)).clamp(0.0, 1.0);
      if (local <= 0) continue;
      canvas.save();
      canvas.translate(p.xRatio * size.width,
          -_startY + local * p.fall * (size.height + _startY + _overshoot));
      canvas.rotate(p.spin * 2 * pi * local);
      canvas.drawRect(
        Rect.fromCenter(center: Offset.zero, width: _pieceW, height: _pieceH),
        Paint()..color = palette[p.colorIndex].withValues(alpha: alpha),
      );
      canvas.restore();
    }
  }

  @override
  bool shouldRepaint(_BurstPainter old) =>
      old.t != t || old.ink != ink || old.accent != accent || old.accentSoft != accentSoft;
}
```

낙하 규칙: 헤더 위 `-20`에서 출발해 헤더 아래 `+40`까지 내려간다. 회전은 진행값에 비례하고, 알파는 `t > 0.7`부터 선형으로 0이 된다. `t`가 바뀔 때만 다시 그린다.

### 4. `UnlockBurst` — 1회만

```dart
class UnlockBurst extends StatefulWidget { const UnlockBurst({super.key}); ... }

class _UnlockBurstState extends State<UnlockBurst> with SingleTickerProviderStateMixin {
  late final AnimationController _c = AnimationController(vsync: this);
  late final Animation<double> _t;
  bool _started = false, _done = false;

  @override
  void didChangeDependencies() {
    // GameMotion.of는 context가 필요해 initState에서 못 쓴다. _started 플래그가
    // "initState에서 한 번"과 같은 보장을 준다 — 리빌드해도 다시 재생하지 않는다.
    super.didChangeDependencies();
    if (_started) return;
    _started = true;
    final motion = GameMotion.of(context);
    _c.duration = motion.celebrate;                  // 감소 모션이면 0 → 즉시 완료
    _t = CurvedAnimation(parent: _c, curve: motion.curveStandard);
    _c.forward().whenComplete(() { if (mounted) setState(() => _done = true); });
  }

  @override
  void dispose() { _c.dispose(); super.dispose(); }

  @override
  Widget build(BuildContext context) {
    if (_done) return const SizedBox.shrink();       // 끝나면 스스로 사라진다
    final c = GameColors.of(context);
    return IgnorePointer(                            // 버튼 탭을 가로채지 않는다
      child: AnimatedBuilder(
        animation: _t,
        builder: (_, _) => CustomPaint(painter: _BurstPainter(
            t: _t.value, ink: c.ink, accent: c.accent, accentSoft: c.accentSoft)),
      ),
    );
  }
}
```

감소 모션에서는 `duration`이 0이라 `forward()`가 바로 완료되고, 다음 프레임에 `SizedBox.shrink()`가 된다.

### 5. `ResultPage` 연결

헤더만 `Stack`으로 감싼다. 목록·버튼은 그대로다.

```dart
justUnlocked
    ? Stack(children: [
        header,
        Positioned.fill(child: const UnlockBurst()),   // 헤더 크기 = 연출 캔버스 크기
      ])
    : header
```

`Stack`의 비위치 자식이 헤더 하나뿐이라 `Stack` 크기는 헤더 크기가 되고, `Positioned.fill`이 그 위를 덮는다. `_BurstPainter`의 `size.height`가 곧 헤더 높이다.

## 테스트 `test/ui/result_screen_test.dart` — 최종 11종

07-06-01이 1~5, 07-06-02가 6~7을 넣었다. 이 문서가 8~11을 넣고 5번을 확장해 표를 확정한다.

| # | 테스트 | 기대 | 넣은 문서 |
|---|---|---|---|
| 1 | `E-06 카운트업 종료값` | `pumpAndSettle()` 후 `'+12'`, `'5 / 7 정답'` | 07-06-01 |
| 2 | `E-06 중간값` | `pump(celebrate ~/ 2)` 시 `'+0'`도 `'+12'`도 아님 | 07-06-01 |
| 3 | `음수 점수: 0에서 시작해 -3으로` | 첫 프레임 `'+0'`, 종료 `'-3'` | 07-06-01 |
| 4 | `링 진행: 종료 시 correct/total` | `progress == 5/7`(±0.001), `total == 0`이면 0 | 07-06-01 |
| 5 | `감소 모션: 첫 프레임에 최종 상태` | 점수 `'+12'` + 모든 행 opacity 1 + **`UnlockBurst`가 `SizedBox.shrink`** | 01·02·**03** |
| 6 | `E-07 순차 등장` | `pump(fast)` 시 첫 행 1, 마지막 행 < 1 | 07-06-02 |
| 7 | `E-07 행이 많아도 총 slow` | 12행, `pump(slow)` 후 전부 1 | 07-06-02 |
| 8 | `E-08 표시 조건` | 1절 표 4행을 그대로 단언 (`find.byType(UnlockBurst)`) | **07-06-03** |
| 9 | `E-08 1회만 재생` | `pumpAndSettle()` 후 `SizedBox.shrink`. 부모 `setState` 강제 후에도 다시 그려지지 않음 | **07-06-03** |
| 10 | `E-08 결정성` | `buildBurstPieces()` 두 번 호출 결과가 `equals`, 색 인덱스별 8개씩 | **07-06-03** |
| 11 | `E-08이 버튼 탭을 막지 않는다` | 해제 상태에서 `pump()` 직후 `'다음 레벨'` 탭 → 콜백 호출 | **07-06-03** |

9번의 "부모 `setState` 강제"는 `StatefulBuilder`로 `ResultPage`를 감싸고 다시 빌드시키는 것으로 충분하다 — `UnlockBurst`의 `State`가 유지되므로 `_started`가 true로 남아 재생되지 않는다.

기존 `submit_test.dart`는 **이 문서에서 손대지 않는다**. 해제 배지 텍스트는 애니메이션 대상이 아니므로 `레벨 해제…` 세 테스트가 그대로 통과한다.

## 검증

```powershell
cd app
dart run tool/check_invariants.dart
flutter analyze
flutter test test/ui/result_screen_test.dart test/ui/submit_test.dart
flutter test
```

## DoD

- [ ] `unlock_burst.dart` 신규, 외부 패키지 0개 (INV-11, N-05)
- [ ] 조각 24개 = `ink`·`accent`·`accentSoft` 각 8개, `Random(42)` 고정 시드
- [ ] 첫 제출 해제 시에만 표시, **1회**, 끝나면 `SizedBox.shrink()`
- [ ] `IgnorePointer`로 감싸 버튼 탭을 막지 않는다
- [ ] 감소 모션에서 즉시 종료
- [ ] `result_screen_test.dart` 11종 전부 통과, `submit_test.dart` 무수정 통과
- [ ] `flutter analyze` 0, `flutter test` 전체 통과, `check_invariants.dart` PASS
- [ ] 07-06 묶음 DoD(부모 문서) 전 항목 충족

## 막히면

- **`pumpAndSettle` 타임아웃** → 컨트롤러가 반복 중이다. `forward()` 한 번만 부르고 `repeat()`을 쓰지 않는다(UI-GUIDE 1절).
- **조각 위치가 실행마다 다름** → `Random()`을 시드 없이 썼다. `Random(42)`만 쓴다.
- **연출이 두 번 재생됨** → `didChangeDependencies`의 `_started` 가드가 빠졌다. 이 콜백은 테마·`MediaQuery`가 바뀔 때마다 다시 불린다.
- **`'다음 레벨'` 탭이 안 먹음** → `IgnorePointer`가 빠졌거나 `Stack`이 헤더가 아니라 화면 전체를 덮었다. `Stack`은 헤더만 감싼다.
- **`shouldRepaint`가 항상 true** → 색을 `List`로 받아 비교했다. 3절처럼 개별 `Color` 필드로 받는다.
- **다크에서 조각이 배경에 묻힘** → 정상 범위다. 색을 새로 만들지 말고 `ink`·`accent`·`accentSoft` 세 토큰만 쓴다(UI-GUIDE 3.3 E-08).

## 다음

[07-07-01.route-transitions.md](07-07-01.route-transitions.md) — **07-04·07-05 묶음이 모두 끝나야 시작한다.** 07-06 묶음만 끝난 상태로는 시작하지 않는다.
