# 07-06-01. ScoreHeader · 점수 카운트업 E-06 · 정답 링

담당: **AGENT**
상위: [07-06.result-screen.md](07-06.result-screen.md) · 계약: [../UI-GUIDE.md](../UI-GUIDE.md) 1절 "축하는 절제", 2.2·2.3·2.4·2.5, 3.1~3.2, 3.3(E-06), 4절
선행: [07-02-05.thememode-and-theme-tests.md](07-02-05.thememode-and-theme-tests.md) · 다음: [07-06-02.word-result-cards.md](07-06-02.word-result-cards.md)
불변: INV-04b, INV-05, INV-08, INV-09, INV-12
커밋: `feat: 결과 화면 점수 헤더 — 카운트업과 정답 링`

07-06 묶음(07-06-01 ~ 07-06-03)은 07-02-05만 끝나면 07-03·07-04·07-05와 **병렬**로 진행할 수 있다.

## 목표

결과 화면 상단의 텍스트 한 줄을 **점수 헤더**로 바꾼다. 점수가 0에서 최종값까지 카운트업하고(E-06), 옆의 링이 정답 비율만큼 채워진다.
단어 목록은 이 문서에서 건드리지 않는다(`ListTile` 그대로). 해제 연출(E-08)은 07-06-03이 얹는다.

## 컨텍스트 (시작 전 읽기)

- `app/lib/ui/result/result_page.dart` — 지금의 헤더 `Column`, `justUnlocked` 계산, `nextLevelOf` 사용 위치
- `app/lib/domain/model/submit_result.dart` — `score`(`correctCount - wrongCount`), `correctCount`, `total`, `isFirstSubmit`. 전부 getter이며 음수 `score`가 정상이다
- `app/lib/domain/model/level_spec.dart` — `clearScore`(기본 0)
- `app/test/ui/submit_test.dart` — `결과 화면 (ResultPage)` 그룹 11종. 헤더 문구를 찾는 곳이 어디인지 확인
- `app/lib/ui/theme/tokens.dart`, `motion.dart` — `GameColors.of`, `GameMotion.of`, `GameSpace`, `GameType`

## 산출물

```
app/lib/ui/result/score_header.dart    신규 — ScoreHeader + _RingPainter
app/lib/ui/result/result_page.dart     헤더 Column → ScoreHeader 한 줄로 교체
app/test/ui/result_screen_test.dart    신규 — 이 문서 몫 5종
```

## 작업

### 1. 바꾸면 안 되는 문자열 확정

현재 코드는 두 값을 **인접 문자열 리터럴로 이어 붙인 `Text` 하나**다.

```dart
'${result.correctCount} / ${result.total} 정답'
'    점수 ${result.score >= 0 ? '+' : ''}${result.score}'   // → '5 / 7 정답    점수 +12'
```

이걸 `Text` 둘로 나눈다. 나눈 뒤에도 아래가 그대로여야 한다.

| 값 | 형식 | 실제 출력 |
|---|---|---|
| 비율 | `'${correct} / ${total} 정답'` | `'5 / 7 정답'` |
| 점수 ≥ 0 | `'+$v'` | 12 → `'+12'`, **0 → `'+0'`** |
| 점수 < 0 | `'$v'` | -3 → `'-3'` |
| 해제 | `'레벨 $unlockedLevelId이 열렸습니다'` | `'레벨 2이 열렸습니다'` (조사는 항상 `이`) |
| 재제출 | `'재제출이라 기록에 반영되지 않았습니다'` | INV-09, 글자 하나도 바꾸지 않는다 |

`'점수 '` 접두사와 사이의 공백 4칸은 사라진다 — INV-09 목록에 없고 `test/**`에서 이 문자열을 찾는 곳이 없음을 확인했다. 반면 `submit_test.dart` 통합 테스트는 `find.textContaining('4 / 4')`를 `findsOneWidget`으로 보므로, 비율 문구는 **단 하나의 `Text`**에만 있어야 한다.

### 2. `score_header.dart`

```dart
// 링 지름 56 / 두께 4는 07-06 부모 문서 도식이 정한 이펙트 내부 상수다.
// 색·시간·타입 스케일만 UI-GUIDE 토큰에서 가져온다(2.2, 2.4, 3.1).
const double _ringSize = 56;
const double _ringStroke = GameSpace.xs; // 4

class ScoreHeader extends StatelessWidget {
  final int score, correct, total;
  final bool isFirstSubmit, justUnlocked;
  final int? unlockedLevelId;
  const ScoreHeader({super.key, required this.score, required this.correct,
      required this.total, required this.isFirstSubmit,
      required this.justUnlocked, this.unlockedLevelId});

  @override
  Widget build(BuildContext context) {
    final c = GameColors.of(context);
    final motion = GameMotion.of(context);
    final ratio = total == 0 ? 0.0 : correct / total;   // total == 0 → 링 진행 0

    return Padding(
      padding: const EdgeInsets.fromLTRB(GameSpace.l, GameSpace.m, GameSpace.l, GameSpace.m),
      child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
        TweenAnimationBuilder<double>(
          tween: Tween(begin: 0, end: 1),
          duration: motion.celebrate,          // 900ms, 감소 모션이면 0
          curve: motion.curveStandard,         // easeOutCubic — 1을 넘지 않는다
          builder: (context, t, _) {
            final shown = (score * t).round();
            return Row(children: [
              ExcludeSemantics(                // 매 프레임 바뀌는 숫자를 읽지 않게
                child: Text('${shown >= 0 ? '+' : ''}$shown',
                    style: GameType.display.copyWith(color: c.ink)),
              ),
              const SizedBox(width: GameSpace.xl),
              SizedBox.square(
                dimension: _ringSize,
                child: CustomPaint(painter: _RingPainter(
                    progress: ratio * t, track: c.line, fill: c.success)),
              ),
              const SizedBox(width: GameSpace.m),
              Text('$correct / $total 정답',
                  style: GameType.label.copyWith(color: c.inkMuted)),
            ]);
          },
        ),
        if (justUnlocked && unlockedLevelId != null) _UnlockBadge(levelId: unlockedLevelId!),
        if (!isFirstSubmit) _ResubmitNote(),
      ]),
    );
  }
}
```

- 헤더 전체를 `Semantics(container: true, label: '점수 $score, $correct / $total 정답')`으로 감싼다. 카운트업 중간값이 스크린리더에 흘러나가지 않게 하려는 것이다.
- **해제 배지와 재제출 안내는 애니메이션 대상이 아니다.** 첫 프레임부터 최종 상태로 보인다 — `submit_test`의 해제·재제출 테스트가 `pump()` 한 번으로 통과해야 한다.
- 둘은 **동시에 나타날 수 없다**(`justUnlocked`는 `isFirstSubmit`을 전제한다). 순서는 도식대로 해제 → 재제출.

### 3. 배지·재제출 안내

| 요소 | 스타일 | 근거 |
|---|---|---|
| 해제 배지 | `accent` 배경 + `GameRadius.pill` + 글자 `GameType.label` 색 **`cellInkOnAccent`** | UI-GUIDE 2.2·2.5. 다크에서 `ink`는 거의 흰색이라 노랑 위 대비가 무너진다. 2.3 대비 계약이 검증하는 쌍이 `cellInkOnAccent / accent`다(라이트에서는 `ink`와 값이 같다) |
| 재제출 안내 | `Icons.info_outline` 16 + `GameType.caption` 색 `inkMuted` | UI-GUIDE 2.4·2.2, 07-06 도식 |

### 4. `_RingPainter`

```dart
class _RingPainter extends CustomPainter {
  final double progress; final Color track, fill;
  const _RingPainter({required this.progress, required this.track, required this.fill});

  @override
  void paint(Canvas canvas, Size size) {
    final center = size.center(Offset.zero);
    final radius = (size.shortestSide - _ringStroke) / 2;
    final base = Paint()..style = PaintingStyle.stroke..strokeWidth = _ringStroke;
    canvas.drawCircle(center, radius, base..color = track);
    if (progress <= 0) return;
    canvas.drawArc(Rect.fromCircle(center: center, radius: radius),
        -pi / 2, 2 * pi * progress, false, base..color = fill..strokeCap = StrokeCap.butt);
  }

  @override
  bool shouldRepaint(_RingPainter old) =>
      old.progress != progress || old.track != track || old.fill != fill;
}
```

클래스는 private이지만 `progress` 필드는 public이라 테스트에서 `(painter as dynamic).progress`로 읽을 수 있다. `dart:math`의 `pi`가 필요하다.

### 5. `result_page.dart` 연결

헤더 `Padding` + 내부 `Column` 전체를 지우고 다음 한 줄로 바꾼다. `justUnlocked`·`next` 계산은 **지금 자리에 그대로 둔다**(도메인 호출은 페이지가 한다).

```dart
ScoreHeader(
  score: result.score, correct: result.correctCount, total: result.total,
  isFirstSubmit: result.isFirstSubmit,
  justUnlocked: justUnlocked, unlockedLevelId: next?.id,
),
```

목록·버튼·`_WordRow`는 이 커밋에서 손대지 않는다.

## 테스트 `test/ui/result_screen_test.dart` (신규, 이 문서 몫 5종)

이 파일은 07-06 세 문서에 걸쳐 자란다. 여기서 뼈대(헬퍼 `_result(score:, correct:, total:)`, `pumpHeader`)를 만들고 5종을 넣는다. 07-06-02가 2종, 07-06-03이 4종을 더해 **최종 11종**이 된다.

| # | 테스트 | 기대 |
|---|---|---|
| 1 | `E-06 카운트업 종료값` | `pumpAndSettle()` 후 `find.text('+12')`, `find.text('5 / 7 정답')` 각 1개 |
| 2 | `E-06 중간값` | `pump()` 후 `'+0'`, `pump(celebrate ~/ 2)` 후 텍스트가 `'+0'`도 `'+12'`도 아님 |
| 3 | `음수 점수: 0에서 시작해 -3으로` | 첫 프레임 `'+0'`, `pumpAndSettle()` 후 `'-3'` |
| 4 | `링 진행: 종료 시 correct/total` | `_RingPainter.progress`가 `5/7`(오차 0.001). `total == 0`이면 0이고 예외 없음 |
| 5 | `감소 모션: 첫 프레임에 최종 점수` | `MediaQuery(data: MediaQueryData(disableAnimations: true))` 아래 `pump()` 한 번에 `'+12'` |

5번은 07-06-02가 행 opacity 단언을, 07-06-03이 `UnlockBurst` 단언을 같은 테스트에 덧붙인다(이름은 유지).
테스트 파일 안에서는 `Duration(milliseconds: …)` 리터럴을 써도 된다 — INV-12는 `lib/ui/**`만 본다. 시간은 `GameMotion.standard.celebrate`로 가져오는 편이 안전하다.

## 검증

```powershell
cd app
dart run tool/check_invariants.dart
flutter analyze
flutter test test/ui/result_screen_test.dart test/ui/submit_test.dart
flutter test
```

## DoD

- [ ] `score_header.dart` 신규, `ResultPage`는 `ScoreHeader` 한 줄만 부른다
- [ ] `'+12'` / `'+0'` / `'-3'` / `'5 / 7 정답'` / `'레벨 2이 열렸습니다'` 형식이 표 그대로
- [ ] 해제 배지·재제출 안내는 첫 프레임부터 보인다 (애니메이션 없음)
- [ ] `total == 0`에서 링 진행 0, 나눗셈 예외 없음
- [ ] 감소 모션에서 첫 프레임이 최종 상태
- [ ] `lib/ui/result/**`에 `Color(0x`·`Colors.`·`Duration(milliseconds` 없음 (INV-05, INV-12)
- [ ] 기존 `submit_test.dart` 11종 무수정 통과, 신규 5종 통과, `flutter analyze` 0

## 막히면

- **`pump()` 한 번인데 이미 `'+12'`** → `TweenAnimationBuilder`는 `initState`에서 `forward()`한다. 첫 프레임은 `begin`(0)이고, 값이 진행하려면 `pump(Duration)`을 줘야 한다. 그래도 종료값이면 `tween`의 `begin`이 `end`와 같다.
- **`'4 / 4'`가 `findsNWidgets(2)`** → 비율 문구가 두 위젯에 있다. 점수 쪽에서 `'/'`를 다시 쓰지 않았는지 본다.
- **다크에서 해제 배지 글자가 안 보임** → `ink`를 썼다. `cellInkOnAccent`로 바꾼다(UI-GUIDE 2.3).
- **`(painter as dynamic).progress`가 `NoSuchMethodError`** → 필드에 `_`를 붙였다. 클래스만 private이고 필드는 public이어야 한다.
- **점수가 마지막에 튄다** → 곡선이 1을 넘었다. E-06은 `curveStandard`(`easeOutCubic`)이고 `curvePop`(`easeOutBack`)을 쓰지 않는다.

## 다음

[07-06-02.word-result-cards.md](07-06-02.word-result-cards.md)
