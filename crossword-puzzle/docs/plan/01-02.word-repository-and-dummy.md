# 01-02. WordRepository 인터페이스 · 더미 사전

담당: **AGENT**
선행: 01-01 · 다음: 01-03
상위: [01-domain-map-generator.md](01-domain-map-generator.md)

## 목표

생성기가 사전을 보는 **유일한 창구**를 정의하고, DB 없이 알고리즘을 돌릴 수 있는 더미 구현 2종을 만든다.
이 인터페이스는 3단계(03-03)가 drift로 구현한다. **계약 문서는 이 파일이다.**

## 컨텍스트

- 01-01 산출물 (`WordEntry`)
- [../DESIGN.md](../DESIGN.md) 4절 (격자 탐색 성능 — `c1..c5` 인덱스 전제)

## 산출물

```
app/lib/domain/repository/
└─ word_repository.dart
app/test/domain/fixtures/
├─ dummy_dictionary.dart      합성 사전 (알고리즘 검증용)
└─ sample_korean_words.dart   실제 한국어 200단어 (눈 검수용)
```

---

## `word_repository.dart`

```dart
import '../model/word_entry.dart';

abstract class WordRepository {
  /// 패턴 질의.
  ///
  /// [length]  단어 음절 수 (2..5)
  /// [tiers]   허용 티어 집합. 비면 전체 티어
  /// [fixed]   고정된 자리. key=0-based 음절 index, value=음절.
  ///           예) length 3, fixed {1: '가'} → `?가?`
  /// [exclude] 제외할 표제어 (이미 격자에 놓인 단어들)
  /// [limit]   최대 반환 개수
  ///
  /// 반환 순서는 **정해지지 않는다.** 호출자가 Random(seed)로 섞어 쓴다.
  /// 구현은 결정적이어야 한다: 같은 인자 → 같은 리스트(같은 순서).
  /// (3단계 주의: SQL `ORDER BY RANDOM()` 금지. 03-03 참조)
  Future<List<WordEntry>> findByPattern({
    required int length,
    Set<int> tiers = const {},
    Map<int, String> fixed = const {},
    Set<String> exclude = const {},
    int limit = 50,
  });

  /// 코어 후보.
  ///
  /// 통계 점수(`correct - wrong * 2`) **낮은 순**, 동점은 [seed] 기반 랜덤.
  /// [count]보다 넉넉히 돌려준다(권장 count * 4). 생성기가 그중에서 고르고
  /// 실패 시 다른 후보로 교체하기 때문이다.
  Future<List<WordEntry>> coreCandidates({
    required int tier,
    required int count,
    required int seed,
  });
}
```

### 비동기인 이유

3단계 구현이 DB I/O라 `Future` 다. 더미 구현은 `Future.value(...)` 로 즉시 완료시킨다.
생성기(01-07)는 `await` 를 쓰되, 루프 안에서 질의를 남발하지 않도록 **슬롯당 1회 질의 후 캐시**한다.

### 결정성 계약 (중요)

같은 `(spec, repository 내용, seed)` → 같은 `Puzzle` 이어야 한다(01-07 DoD).
그러려면 `findByPattern` 이 **같은 인자에 같은 순서**를 돌려줘야 한다.
정렬 키는 구현이 정하되 안정적이면 된다. 더미는 `headword` 사전순으로 고정한다.

---

## `dummy_dictionary.dart`

목적: **음절 풀을 좁게 만들어** 패턴이 잘 맞게 한다. 알고리즘 자체의 버그를 드러내는 게 목적이지
실데이터 난이도 재현이 아니다. 실데이터 실패율은 3단계(03-05)에서 따로 잰다.

```dart
import 'dart:math';
import 'package:jgame/domain/model/word_entry.dart';
import 'package:jgame/domain/repository/word_repository.dart';

/// 메모리 기반 WordRepository. 정렬은 headword 사전순으로 고정(결정성).
class InMemoryWordRepository implements WordRepository {
  final List<WordEntry> _words;
  final Map<String, int> _stats; // headword -> correct - wrong*2

  InMemoryWordRepository(List<WordEntry> words, {Map<String, int>? stats})
      : _words = List.of(words)..sort((a, b) => a.headword.compareTo(b.headword)),
        _stats = stats ?? const {};

  @override
  Future<List<WordEntry>> findByPattern({
    required int length,
    Set<int> tiers = const {},
    Map<int, String> fixed = const {},
    Set<String> exclude = const {},
    int limit = 50,
  }) async {
    final out = <WordEntry>[];
    for (final w in _words) {
      if (w.length != length) continue;
      if (tiers.isNotEmpty && !tiers.contains(w.tier)) continue;
      if (exclude.contains(w.headword)) continue;
      var ok = true;
      for (final e in fixed.entries) {
        if (w.syllableAt(e.key) != e.value) { ok = false; break; }
      }
      if (!ok) continue;
      out.add(w);
      if (out.length >= limit) break;
    }
    return out;
  }

  @override
  Future<List<WordEntry>> coreCandidates({
    required int tier,
    required int count,
    required int seed,
  }) async {
    final pool = _words.where((w) => w.tier == tier).toList();
    final rnd = Random(seed);
    // 동점 랜덤: 사전순 정렬된 풀을 섞은 뒤 점수로 안정 정렬
    pool.shuffle(rnd);
    pool.sort((a, b) =>
        (_stats[a.headword] ?? 0).compareTo(_stats[b.headword] ?? 0));
    return pool.take(count * 4).toList();
  }
}

/// 합성 더미 사전 생성기.
/// 음절 풀이 좁아 교차가 잘 맞는다. 티어는 피라미드 분포.
List<WordEntry> buildDummyDictionary({int seed = 1, int target = 4000}) {
  const pool = [
    '가','나','다','라','마','바','사','아','자','차',
    '카','타','파','하','고','노','도','로','모','보',
    '소','오','조','초','기','니','디','리','미','비',
    '시','이','지','치','구','누','두','루','무','부',
  ];
  const tierRatio = [25, 22, 18, 14, 10, 7, 4]; // 02-08과 같은 피라미드
  final rnd = Random(seed);
  final seen = <String>{};
  final out = <WordEntry>[];

  while (out.length < target) {
    final len = 2 + rnd.nextInt(4); // 2..5
    final buf = StringBuffer();
    for (var i = 0; i < len; i++) {
      buf.write(pool[rnd.nextInt(pool.length)]);
    }
    final hw = buf.toString();
    if (!seen.add(hw)) continue;

    var roll = rnd.nextInt(100);
    var tier = 1;
    for (var t = 0; t < tierRatio.length; t++) {
      if (roll < tierRatio[t]) { tier = t + 1; break; }
      roll -= tierRatio[t];
      tier = t + 2;
    }
    if (tier > 7) tier = 7;

    out.add(WordEntry(headword: hw, tier: tier, pos: '명사'));
  }
  return out;
}
```

**주의**: `buildDummyDictionary` 는 `seed` 고정 시 항상 같은 사전을 낸다. 테스트는 이 성질에 의존한다.

---

## `sample_korean_words.dart`

눈으로 결과를 확인할 용도. 실제 한국어 명사 **200개**를 손수 나열한다.

```dart
import 'package:jgame/domain/model/word_entry.dart';

/// 실제 한국어 명사 샘플. 2~5음절, 티어는 대략적인 체감 난이도.
/// 알고리즘 성능 지표로 쓰지 않는다 — 눈으로 결과를 볼 때만 쓴다.
final sampleKoreanWords = <WordEntry>[
  WordEntry(headword: '사과', tier: 1, pos: '명사'),
  WordEntry(headword: '나무', tier: 1, pos: '명사'),
  WordEntry(headword: '사자', tier: 1, pos: '명사'),
  WordEntry(headword: '과자', tier: 1, pos: '명사'),
  WordEntry(headword: '나비', tier: 1, pos: '명사'),
  // ... 총 200개
];
```

작성 요령 — **교차가 생기도록 음절을 겹치게 고른다**:

- `사`로 시작/끝나는 단어 여러 개 (`사과`, `사자`, `사람`, `회사`, `농사`)
- `과`, `나`, `무`, `자`, `장`, `구`, `기`, `산`, `물`, `수` 같은 흔한 음절을 공유하는 묶음
- 길이 분포: 2음절 90개, 3음절 70개, 4음절 30개, 5음절 10개
- 티어 분포: 1~3에 몰아준다 (더미 사전과 달리 이건 채움용 풀 역할)

---

## 테스트

`app/test/domain/repository_test.dart`

```dart
void main() {
  final repo = InMemoryWordRepository(buildDummyDictionary(seed: 1));

  test('findByPattern이 고정 자리를 지킨다', () async {
    final r = await repo.findByPattern(length: 3, fixed: {1: '가'});
    expect(r, isNotEmpty);
    for (final w in r) {
      expect(w.length, 3);
      expect(w.syllableAt(1), '가');
    }
  });

  test('findByPattern이 티어·제외를 지킨다', () async {
    final r = await repo.findByPattern(
      length: 2, tiers: {1, 2}, exclude: {'가나'}, limit: 10);
    expect(r.length, lessThanOrEqualTo(10));
    for (final w in r) {
      expect(w.tier, anyOf(1, 2));
      expect(w.headword, isNot('가나'));
    }
  });

  test('findByPattern은 같은 인자에 같은 순서를 낸다', () async {
    final a = await repo.findByPattern(length: 3, tiers: {2});
    final b = await repo.findByPattern(length: 3, tiers: {2});
    expect(a.map((w) => w.headword).toList(),
           b.map((w) => w.headword).toList());
  });

  test('coreCandidates는 통계 낮은 순', () async {
    final r = InMemoryWordRepository(
      buildDummyDictionary(seed: 1),
      stats: {/* 특정 단어에 높은 점수 부여 */},
    );
    final c = await r.coreCandidates(tier: 3, count: 5, seed: 42);
    expect(c.length, greaterThanOrEqualTo(5));
    expect(c.every((w) => w.tier == 3), isTrue);
    // 점수 높게 준 단어가 앞쪽에 없는지 확인
  });

  test('coreCandidates는 같은 seed에 같은 결과', () async {
    final a = await repo.coreCandidates(tier: 3, count: 5, seed: 7);
    final b = await repo.coreCandidates(tier: 3, count: 5, seed: 7);
    expect(a.map((w) => w.headword).toList(),
           b.map((w) => w.headword).toList());
  });

  test('더미 사전이 seed 고정 시 재현된다', () {
    final a = buildDummyDictionary(seed: 1);
    final b = buildDummyDictionary(seed: 1);
    expect(a.map((w) => w.headword).toList(),
           b.map((w) => w.headword).toList());
  });

  test('더미 사전 티어 분포가 피라미드', () {
    final d = buildDummyDictionary(seed: 1, target: 4000);
    final counts = List.filled(8, 0);
    for (final w in d) counts[w.tier]++;
    for (var t = 1; t < 7; t++) {
      expect(counts[t], greaterThan(counts[t + 1]),
          reason: 'tier $t should be more common than ${t + 1}');
    }
  });
}
```

## 검증

```powershell
dart analyze
flutter test test/domain/repository_test.dart
Select-String -Path app/lib/domain -Include *.dart -Recurse -Pattern "dart:io|package:flutter|package:drift"
```

기대: analyze 이슈 0, 테스트 전부 통과, import 위반 0건.

## DoD

- [ ] `word_repository.dart` 에 위 두 메서드가 문서 주석과 함께 존재
- [ ] `InMemoryWordRepository` 가 결정적으로 동작 (같은 인자 → 같은 순서)
- [ ] `buildDummyDictionary` 가 4000단어 생성, 티어 피라미드 분포
- [ ] `sampleKoreanWords` 200개 작성
- [ ] 테스트 7종 통과
- [ ] `domain/` 플랫폼 import 0건

## 막히면

- **더미 사전 티어 분포 테스트 실패** → `tierRatio` 롤 계산 로직 확인. 누적 비교로 단순화해도 된다.
- **`package:jgame/...` import가 안 잡힘** → `pubspec.yaml` 의 `name:` 값 확인. `flutter create --project-name jgame` 을 안 썼으면 실제 이름으로 바꾼다.
- **200단어 수집이 지루함** → 100개로 시작하고 04단계 전에 채운다. 단 DoD는 미충족으로 명시.
